# 代码说明

## 包结构说明

### chartspy包

目前封装echarts、g2plot、KLineChart、HighCharts、Tabulator 对应5个对象

#### Echarts

**参数说明:**
```python
"""
:param options: python词典类型的echarts option
:param extra_js: 复杂图表需要声明定义额外js函数的，通过这个字段传递
:param with_gl: 是否加载echarts-gl扩展，用于3D图表
:param width: 输出div的宽度 支持像素和百分比 比如800px/100%
:param height: 输出div的高度 支持像素和百分比 比如800px/100%
"""
```

**输出:**

render_notebook() 输出到notebook

render_jupyterlab() 输出到jupyterlab

render_html() 输出到Html字符串

render_html_fragment() 输出html片段，方便一个网页输出多个图表

dump_options() 输出到js配置字符串(用于粘贴到Html中)

print_options() 格式化打印options，方便二次修改

**静态方法:**

timeline() 时间轴动画图表

**实例方法:**

overlap_series() 叠加其他配置中的Series数据到现有配置

#### G2PLOT

**参数说明:**
```python
"""
:param data: Dataframe 或者list
:param plot_type: 对应g2plot 图表类型
:param options: g2plot配置文件    
:param extra_js: 复杂图表需要声明定义额外js函数的，通过这个字段传递
:param width: 输出div的宽度 支持像素和百分比 比如800px/100%
:param height: 输出div的高度 支持像素和百分比 比如800px/100%
"""
```

**输出:**

render_notebook() 输出到notebook

render_jupyterlab() 输出到jupyterlab

render_html() 输出到Html字符串

render_html_fragment() 输出html片段，方便一个网页输出多个图表

dump_options() 输出到js配置字符串(用于粘贴到Html中)

print_options() 格式化打印options，方便二次修改

#### KlineCharts
**参数说明**
```python
"""
:param df: Dataframe  columns=[open,high,low,close,volume,timestamp,turnover]
:param mas: 均线参数列表 默认[5, 10, 30, 60, 120, 250]
:param main_indicators: 主图显示的指标列表 MA,EMA,SMA,BOLL,SAR,BBI
:param bottom_indicators: 副图显示指标列表 VOL,MACD,KDJ,RSI,BIAS等
:param df_segments: 线段数据 columns=[start_time,start_price,end_time,end_price]
:param width: 宽度 默认100%
:param height: 高度 默认500px 
"""
```
**输出:**

render_notebook() 输出到notebook

render_jupyterlab() 输出到jupyterlab

render_html() 输出到Html字符串

render_html_fragment() 输出html片段，方便一个网页输出多个图表

#### HighCharts

**参数说明:**
```python
"""
:param options: python词典类型的highcharts option
:param extra_js: 复杂图表需要声明定义额外js函数的，通过这个字段传递
:param width: 输出div的宽度 支持像素和百分比 比如800px/100%
:param height: 输出div的高度 支持像素和百分比 比如800px/100%
"""
```

**输出:**

render_notebook() 输出到notebook

render_jupyterlab() 输出到jupyterlab

render_html() 输出到Html字符串

render_html_fragment() 输出html片段，方便一个网页输出多个图表

dump_options() 输出到js配置字符串(用于粘贴到Html中)

print_options() 格式化打印options，方便二次修改

#### Tabulator

**参数说明:**
```python
"""
:param df: Dataframe 数据
:param sparkline_dict: 迷你图配置 {'col':'line/bar/tristate/discrete/bullet/pie/box'}
:param width_dict: 列宽度配置 {'col':'120'}
:param formatter_dict: 格式化配置 {'col':'progress/star/tickCross/color'}
:param height: 表格高度 默认500
"""
```

**输出:**

render_notebook() 输出到notebook

render_jupyterlab() 输出到jupyterlab

render_html() 输出到Html字符串

render_html_fragment() 输出html片段，方便一个网页输出多个图表

**静态方法:**

reduce_dataframe() 将DataFrame按索引或列方向合并值到数组

#### Tools

Tools的静态方法:

* convert_js_to_dict JavaScript配置转换成python配置
* convert_dict_to_js python配置转换成JavaScript配置 
* convert_to_list 辅助用户把DataFrame,Series,ndarray转换成list结构
* df_uniformize_datetime_columns 统一DataFrame时间相关列的格式
* df2tree 将DataFrame转换成echarts需要的树形结构

### chartspy.express包

pandas DataFrame 数据可视化工具，仿照plotly.express

#### Echarts图表

包含常用 scatter, line, bar, pie, candlestick, heatmap, calendar_heatmap, parallel, radar, theme_river, sankey, sunburst, scatter3d, bar3d, drawdown 等常用图表

#### G2Plot图表

包含 area, bullet, chord, waterfall, liquid, wordcloud, column_stack, column_stack_percent, treemap 等图表

#### HighCharts图表

包含 streamgraph, dependency_wheel, arcdiagram 等图表
